# dpub - discourse (and github) publishing tools

dpub lets you push and pull files, to and from github or discourse.

xpub uses an experimental natural language interface, but otherwise does pretty much the same things as dpub (YMMV).